<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground Service (Service-ready 전환) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Android 13+ 알림 권한 (Foreground Service 알림 표시용) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11 이하 (Note 9 등): 레거시 BLE 권한 -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12+ (S23 Ultra 등): 신권한 -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- BLE 스캔용 위치 권한 (Android 11 이하 필수) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- UWB (Phase 3) -->
    <uses-feature
        android:name="android.hardware.uwb"
        android:required="false" />
    <uses-permission android:name="android.permission.UWB_RANGING" />

    <!-- 수업 시작 5분 전 알림 (Stage 4) -->
    <!-- Android 12+ 정확 알람. SCHEDULE_EXACT_ALARM은 사용자가 Settings에서 허용,
         USE_EXACT_ALARM은 Android 13+ 알람/캘린더 앱용 자동 부여 시도. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 재부팅 후 시간표 알람 자동 재등록 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attendance"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        >
        <!-- 출석 Foreground Service (교수/학생 분리) -->
        <service
            android:name=".service.ProfessorAttendanceService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name=".service.StudentAttendanceService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 수업 시작 5분 전 알림 (Stage 4) -->
        <!-- ScheduleAlarmManager의 AlarmManager 알람을 수신.
             앱 내부에서만 trigger되므로 exported=false. -->
        <receiver
            android:name=".schedule.receiver.ClassReminderReceiver"
            android:exported="false" />

        <!-- 부팅 후 시간표 알람 재등록 (Stage 4) -->
        <!-- BOOT_COMPLETED는 시스템이 보내는 broadcast이므로 exported=true. -->
        <receiver
            android:name=".schedule.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>